@using Entities.Concrete.DynamicDataEntity
@model WorkingStatus
@{
    ViewBag.Title = "WorkingStatusGetById";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h3>Çalışma Durumu Güncelle</h3>
@using (Html.BeginForm("WorkingStatusUpdate", "WorkingStatus", FormMethod.Post))
{
    <div class="box box-primary">
        <div class="box-header with-border">
            <table id="example2" class="table table-bordered table-hover dataTable" role="grid" aria-describedby="example2_info">
                <thead>

                </thead>
                <tbody>
                    <tr role="row" class="odd">
                        <td class="sorting_1"><b>Çalışma Durumu</b></td>
                        <td>
                            @Html.HiddenFor(x => x.IstihtamDurumId)
                            @Html.TextBoxFor(x => x.IstihtamDurumu, null, new { @class = "form-control" })
                        </td>
                    </tr>
            </table>
            <button class="btn btn-warning">Güncelle</button>
        </div>
    </div>
}

