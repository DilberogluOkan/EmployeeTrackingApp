
@using Entities.Concrete.DynamicDataEntity
@model DesignationStatus
@{
    ViewBag.Title = "DesignationStatusGetById";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Nakil Durum Güncelle</h3>
@using (Html.BeginForm("DesignationStatusUpdate", "DesignationStatus", FormMethod.Post))
{
    <div class="box box-primary">
        <div class="box-header with-border">
            <table id="example2" class="table table-bordered table-hover dataTable" role="grid" aria-describedby="example2_info">
                <thead>

                </thead>
                <tbody>
                    <tr role="row" class="odd">
                        <td class="sorting_1"><b>Nakil Durum</b></td>
                        <td>
                            @Html.HiddenFor(x => x.NakilDurumId)
                            @Html.TextBoxFor(x => x.NakilDurumu, null, new { @class = "form-control" })
                        </td>
                    </tr>
            </table>
            <button class="btn btn-warning">Güncelle</button>
        </div>
    </div>
}
